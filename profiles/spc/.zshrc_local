eval "$(atuin init zsh --disable-up-arrow --disable-ctrl-r)"
eval "$(atuin-fzf --zsh)"

# Disabled appendWindowsPath, so explicitly add System32 for cmd.exe, clip.exe etc.
export PATH="$PATH:/mnt/c/Windows/System32"
