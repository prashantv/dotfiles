[alias]
  a = add
	b     = branch
  ap = add -p
  cb = checkout -b
  ci = commit
  co = checkout
  d = diff
  ds = diff --stat
  dc = diff --cached
  dw = diff -w
	l     = log --graph
	lp    = log --graph --first-parent
	st    = status

  # log with local times
  llog = log --date=local
  # what files are changed
  changes  = diff --name-status -r

[user]
	email = prashant@uber.com
	name = Prashant Varanasi
[branch]
	autosetuprebase = always
[rerere]
	enabled = true
[push]
	default = simple
[filter "lfs"]
	clean = git lfs clean %f
	smudge = git lfs smudge %f
	required = true
[help]
	autocorrect = 5

# Allow overrides for local environment (like user)
[include]
  path = .gitconfig_local
[difftool "sourcetree"]
	cmd = opendiff \"$LOCAL\" \"$REMOTE\"
	path =
[mergetool "sourcetree"]
	cmd = /Applications/SourceTree.app/Contents/Resources/opendiff-w.sh \"$LOCAL\" \"$REMOTE\" -ancestor \"$BASE\" -merge \"$MERGED\"
	trustExitCode = true
[http "https://gopkg.uberinternal.com"]
	followRedirects = true
[http "https://gopkg.in"]
	followRedirects = true

[core]
	excludesfile = /home/prashant/.gitignore_global
	compression = 9
[url "ssh://code.uber.internal/"]
	insteadOf = https://code.uber.internal/
[url "ssh://config.uber.internal/"]
	insteadOf = https://config.uber.internal/

