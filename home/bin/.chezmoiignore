{{ if not (get .features "age") }}
age
age-keygen
pkgs/age
{{ end }}

{{ if not (get .features "bat") }}
bat
pkgs/bat
{{ end }}

{{ if not (get .features "delta") }}
delta
pkgs/delta
{{ end }}

{{ if not (get .features "fd") }}
fd
pkgs/fd
{{ end }}

{{ if not (get .features "nvim") }}
nvim
pkgs/nvim
{{ end }}

{{ if not (get .features "rg") }}
rg
pkgs/ripgrep
{{ end }}

{{ if not (get .features "yq") }}
yq
{{ end }}
